import 'package:flutter/material.dart';

class TeachingPray extends StatelessWidget {
  TeachingPray({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('TeachingPray'),
      ),
      body: Center(
        child: Text('TeachingPray'),
      ),
    );
  }
}
